{
  "engine": "oscilloscope",
  "title": "Oscilloscope",
  "tagline": "A simulated CRT screen — a phosphor persistence buffer, a film-off-a-real-screen filter, and a glitch layer.",
  "method": "The core is a phosphor persistence buffer. The electron beam is stroked each frame into an intensity layer that is added to a float buffer which decays a little every frame, exactly as P31 green afterglow does. That decay is what produces the orbiting ghost trails as a figure morphs or precesses, and faster beam travel means a dimmer trace (intensity goes as 1/segment-length), so Lissajous corners glow the way they do on real hardware. The buffer is colourised and given an additive gaussian bloom halo over a static graticule. On top of that sit two independent post layers — a three-stage physical model of filming a CRT with a camera, and an acquisition-fault layer — plus a video-to-ASCII front end that feeds the same filter.",
  "output": {
    "native": "1080x1920 @60",
    "note": "The scope face is a rectangular graticule with square pixel cells, sized as a FRACTION of the frame (`face_w_frac`/`face_h_frac`), and `to_px` is deliberately anisotropic so vertical deflection fills a tall frame. For a landscape screen, set those fractions and the frame size and the mapping follows; nothing else changes. The film and glitch filters are resolution-agnostic and re-seed their statics per size."
  },
  "modules": [
    {"path": "osc/scope.py", "kind": "core", "what": "The CRT. RGB persistence buffer, `to_px` (normalised to the screen face), the static graticule in three crossfadeable states, `beam()` stroking with 1/speed shading, `beam_transient()` for non-persistent overlays, and the compose pass. A negative grid state means no graticule at all."},
    {"path": "osc/crtfilm.py", "kind": "post", "what": "`FilmLook` — filming a CRT with a camera, in three physically ordered stages. SCREEN in linear light (multi-scale glass halation, static phosphor grain in lit areas only, dust and smudges that light up under the trace, glass sheen); LENS (barrel distortion, transverse chromatic aberration per channel, corner defocus, vignette, sub-pixel hand-shake); CAMERA (exposure flicker, a drifting hum band, Reinhard-extended highlight blowout, tinted black lift, fine and coarse-chroma sensor noise). All statics are seeded and precomputed per resolution."},
    {"path": "osc/glitchfx.py", "kind": "post", "what": "`GlitchFX` — an error-prone acquisition layer, split in two. `pre()` applies SIGNAL faults before the film pass so the camera films them (sync-tear band slips, vertical strip static with per-row jitter, drifting RF herringbone, 1–3 frame dropouts); `post()` applies SENSOR defects after it (fixed dead pixels, flickering hot pixels). Poisson-scheduled and seeded, with one of each forced early so a short sample shows everything."},
    {"path": "osc/asciivid.py", "kind": "front-end", "what": "Video frame to coloured ASCII, vectorised. Luminance picks a glyph from a ramp, the SOURCE pixel colour is kept, near-black cells are dropped — and a whole frame is assembled at once through a precomputed glyph atlas and fancy indexing rather than a per-cell blit loop, which is what makes it fast enough for video."},
    {"path": "osc/track.py", "kind": "front-end", "what": "Subject tracking for the ASCII crop. Asks WHICH WINDOW HOLDS THE MOST ACTION rather than where the mean is — the centroid of a saliency map barely moves (0.491 to 0.504 over a six-second test, i.e. the fixed centre crop with extra steps), because energy is spread broadly and roughly symmetrically across a frame."},
    {"path": "osc/audio.py", "kind": "audio", "what": "Audio decode, per-band FFT magnitudes, and `mux_audio`. The sync rule is one line: build `start` must equal mux `-ss`."},
    {"path": "osc/render.py", "kind": "render", "what": "Frame clock, beam strokes, ffmpeg rawvideo pipe, atomic output. Honours per-composition decay, grid state, transient flash traces, and `warmup` (loop pre-roll)."},
    {"path": "osc/config.py", "kind": "config", "what": "`RenderConfig` (resolution, fps, decay, beam, glow, colours), the graticule palette, ffmpeg resolution, the NVENC probe."},
    {"path": "osc/gpu.py", "kind": "backend", "what": "CuPy backend with automatic numpy fallback. Backs the scope buffers, bloom, compose and the whole film filter; measured about 10x on render and 4x on the filter. Statics are generated from the seed so the look is identical CPU or GPU."},
    {"path": "ascii_video.py", "kind": "pipeline", "what": "The full video-to-ASCII-to-CRT pipeline, end to end. Decode, ASCII, film, encode, and mux the source audio window back on."},
    {"path": "filter_cli.py", "kind": "tool", "what": "Apply the film filter (and optionally the glitch layer) to ANY existing mp4. Probes size and fps, streams decode to process to encode, copies the source audio window. This is the most directly reusable thing in the engine — it needs nothing else from the repository."}
  ],
  "objects": [
    {
      "group": "The CRT core",
      "items": [
        {"name": "Scope", "signature": "Scope(cfg)", "what": "The screen. Owns the RGB persistence buffer and the transient head buffer.", "notes": "The persistence buffer is RGB, not grayscale, so traces can be any colour rather than only phosphor green."},
        {"name": "beam", "signature": "beam(pts, gain=1.0, color=None, width=None, speed_shade=True)", "what": "Stroke a polyline into the PERSISTENT buffer, shaded by 1/segment-length.", "notes": "Never use this for a flashed overlay — persistence burns a static bright shape in for the better part of a second of decay."},
        {"name": "beam_transient", "signature": "beam_transient(pts, gain, color)", "what": "Stroke into a transient buffer that is cleared every frame.", "notes": "The correct route for anything that must appear and vanish with no burn-in."},
        {"name": "head", "signature": "head(p)", "what": "The hot beam dot, drawn into its own transient buffer.", "notes": "Kept separate so the fast dot never smears into a chain of beads. The compose pass skips the head bloom entirely when the buffer is empty, which is most of the cost saved on dot-free compositions."},
        {"name": "graticule states", "signature": "scope.render(grid_state)", "what": "Three states crossfaded by a float: cartesian (0), radial rings and spokes (1), axes-only faint cross (2). Negative means none.", "notes": "The scope's own `_grats` list can be replaced to offer a different set."}
      ]
    },
    {
      "group": "The film filter",
      "intro": "A three-stage physical model, in the order light actually goes through the system. Every hue behaviour is parameterised with GREEN defaults, so existing green renders are byte-for-byte unchanged when a new option is added.",
      "items": [
        {"name": "FilmLook", "signature": "FilmLook(w, h, fps, n_frames, seed=7, **phosphor_cfg)", "what": "The whole filter. Absolute-frame indexed and fixed-seed, so a clip rendered in chunks concatenates seamlessly.", "notes": "`exposure` defaults to 1.75, which is tuned for a thin bright trace on black. A full-frame bright image needs about 0.72 or the whole field blows out to pastel."},
        {"name": "film_config_for_phosphor", "signature": "film_config_for_phosphor(color, halation_boost=1.0)", "what": "Derives the filter's hue behaviour from a trace colour: blowout keys off the LIT channels and bleeds into the DEFICIENT ones, so ANY hue blows to white, and blacks, dust and sheen tint toward the hue.", "notes": "Pure and cheap, so it can be called per frame. This is what makes a dynamic-colour composition possible with no hand-tuning of the filter."},
        {"name": "ambilight", "signature": "FilmLook(ambient_gain=...)", "what": "A very wide colour-PRESERVING blur of the lit screen added into the surround, so content's own colour spills through the glass into the dark margins.", "notes": "A cyan face glows cyan rather than producing a neutral white sheen. Defaults to off so existing renders are unchanged."},
        {"name": "bevel", "signature": "FilmLook(bevel=..., bevel_frac=..., bevel_tint=...)", "what": "A tinted bevelled edge that seats flat glass into a bezel.", "notes": "Defaults to off."},
        {"name": "barrel_k", "signature": "FilmLook(barrel_k=...)", "what": "Glass bulge.", "notes": "0.03 is a flat CRT; 0.05 a subtle main-face bow; 0.12 an obvious glass bulge."},
        {"name": "GlitchFX", "signature": "GlitchFX(w, h, fps, n_frames, level=0.8, seed=...)", "what": "The acquisition-fault layer. `level` scales both rate and strength.", "notes": "About 0.8 is subtle, 1.8 heavy. Strips are deliberately NARROW (1.5–5% of width); the first build used 8–22% and read as far too wide."}
      ]
    },
    {
      "group": "The ASCII front end",
      "items": [
        {"name": "AsciiVideo", "signature": "AsciiVideo(w, h, font_px=..., crop_frac=1/3, hsqueeze=0.8, margin=...)", "what": "Video frames to coloured ASCII through a precomputed glyph atlas.", "notes": "Subject-fit: takes a centred slice of the source WIDTH and squeezes it horizontally — the same pixels at a narrower display aspect — so the slice stands tall and fills a vertical screen. `crop_frac=1.0` uses the whole frame."},
        {"name": "subject_pan", "signature": "subject_pan(frames, ...)", "what": "Slides the crop window to follow the action rather than sitting at the centre.", "notes": "Measures which WINDOW holds the most energy, not where the centroid is — see the module note."},
        {"name": "neutral phosphor for ASCII", "signature": "film_config_for_phosphor((255,255,255))", "what": "Full-colour ASCII must be filmed with a NEUTRAL white-phosphor config so every hue halates correctly.", "notes": "Feeding it the green defaults wrecks a multi-colour image."}
      ]
    },
    {
      "group": "Audio analysis",
      "items": [
        {"name": "decode_mono / decode_stereo", "signature": "decode_mono(path, start, dur)", "what": "Decode a window of a track to samples via ffmpeg.", "notes": ""},
        {"name": "band magnitudes", "signature": "audio.bands(...)", "what": "Per-band FFT magnitudes over log-spaced frequency bins.", "notes": "Log spacing matters: pitch is logarithmic, so linear bands put almost every musical note in the bottom bin."},
        {"name": "mux_audio", "signature": "mux_audio(video, song, start, out)", "what": "Mux the same window back over a silent render.", "notes": "Build `start` must equal mux `-ss`. This is the entire sync contract."}
      ]
    }
  ],
  "scenes_intro": "The compositions are not shipped. They are catalogued by OUTPUT FAMILY — the way the author organises them on disk — because each family is a different reusable format rather than a different picture.",
  "scenes": [
    {"name": "look/dimensional", "what": "The flagship. Winds up from the origin into a two-lobe figure, demonstrates angular nodes reflecting through the origin at 2 to 5 lobes morphing CONTINUOUSLY (signed-radius roses cross through the origin so the trace never disconnects), then collapses; then s, p, d and d_z2 orbitals each emerge from the origin, hold, and collapse back before the next. The clip loops — the final collapse plus a decay tail makes the last frame match the first.", "status": "shipped 37 s", "frames": ["look_dimensional_t012.jpg", "look_dimensional_t035.jpg", "look_dimensional_t062.jpg", "look_dimensional_t088.jpg"]},
    {"name": "look/dimensional2d", "what": "The 2-D extended cut as a SEAMLESS 27 s loop: 3-rose, 4-rose, 5-rose, 7-rose, harmonic superposition, a 3:2 Lissajous knot, a 5/2 rational rose, back to 3-rose. Opens already-formed.", "status": "shipped", "frames": ["look_dimensional2d_loop_t005.jpg", "look_dimensional2d_loop_t030.jpg", "look_dimensional2d_loop_t055.jpg", "look_dimensional2d_loop_t080.jpg", "look_dimensional2d_t045.jpg"]},
    {"name": "look/dimensional2d_67", "what": "The same loop with a seven-segment '67' appearing for half a second via the transient buffer, so it vanishes with NO burn-in and the trace under it dims 70%.", "status": "shipped", "frames": ["look_dimensional2d_loop_67_t003.jpg", "look_dimensional2d_loop_67_t050.jpg"]},
    {"name": "look/loop_gears", "what": "Spirograph and guilloche rings — epitrochoids, star gears, lace. One of three 20 s seamless-loop journeys built on the same loop recipe with new shape content.", "status": "shipped, clean and filmed", "frames": ["look_loop_gears_t030.jpg", "look_loop_gears_t070.jpg", "look_loop_gears_filmed_t050.jpg"]},
    {"name": "look/loop_weave", "what": "Lissajous-lattice knots with harmonics.", "status": "shipped, clean and filmed", "frames": ["look_loop_weave_t030.jpg", "look_loop_weave_t070.jpg", "look_loop_weave_filmed_t050.jpg"]},
    {"name": "look/loop_braid", "what": "Counter-rotating three-term orbital braids.", "status": "shipped, clean and filmed", "frames": ["look_loop_braid_t030.jpg", "look_loop_braid_t070.jpg", "look_loop_braid_filmed_t050.jpg"]},
    {"name": "stack/spectral_stack", "what": "Eighteen traces at FIXED heights, one per log-spaced band from 40 Hz to 8 kHz, bass at the bottom and treble at the top, so each region of the screen vibrates audibly synced to its slice of the mix. Each band draws its real band-passed CARRIER with the envelope divided out and scrolled slowly (3.5 down to 1.2 drawn cycles per second — hugely time-stretched at high frequency), sampled with cubic interpolation at float positions. The LIVE envelope sets each trace's height and heat, so the swell stays perfectly synced while the wiggle stays watchable.", "status": "shipped across 19 songs, clean and filmed, in green and pink", "frames": ["stack_spacesong_t045.jpg", "stack_afterdark_t045.jpg", "stack_duvet_t045.jpg", "stack_feelgoodinc_t045.jpg", "stack_summer2000_t045.jpg", "stack_bluecoupe_t045.jpg", "stack_spacesong_filmed_t045.jpg", "stack_green_variant_t045.jpg", "stack_pink_variant_t045.jpg", "look_spectral_stack_t040.jpg"]},
    {"name": "circle_format", "what": "A SAVED, reusable split format: an ASCII album cover in the top half, three overlaid scope circles in the bottom. Each ring traces the song's real band-passed WAVEFORM wrapped once around it — a true scope-XY read, detrended so the ends meet — so the shape genuinely redraws with the audio: bass punches a few big lobes, treble scatters fine spikes. Bass ring at the back and largest, treble at the front, smallest and brightest. Rings are built in round x-space then squashed by the face anisotropy so they render as TRUE circles. The CRT filter tracks the trace colour automatically.", "status": "shipped 20 s", "frames": ["circle_format_t020.jpg", "circle_format_t050.jpg", "circle_format_t080.jpg", "circle_format_v1_t050.jpg"]},
    {"name": "ascii/hers_ascii", "what": "The video-to-ASCII-to-CRT pipeline end to end: a source clip decoded, converted to coloured ASCII with subject tracking, put through the film filter with a neutral white phosphor, and re-muxed with its own audio window.", "status": "shipped", "frames": ["ascii_hers_t015.jpg", "ascii_hers_t045.jpg", "ascii_hers_t078.jpg"]},
    {"name": "test/filmtest", "what": "The film-filter A/B set — before, after, side by side, and three strengths of the same pass. The most useful thing in this list if you are deciding how hard to push `filter_cli.py`.", "status": "reference", "frames": ["test_filmtest_before_t050.jpg", "test_filmtest_after_t050.jpg", "test_filmtest_sidebyside_t050.jpg", "test_filmtest_method_subtle_t050.jpg", "test_filmtest_method_tuned_t050.jpg", "test_filmtest_method_heavy_t050.jpg"]},
    {"name": "test/radial_sound", "what": "The node convention driven by a song: signed radius is a breathing base ring plus per-band petal harmonics, with bass-onset detection triggering a fast 180 deg orientation flip and an outward size pulse per beat.", "status": "preview", "frames": ["test_radial_sound_t035.jpg", "test_radial_sound_t075.jpg"]},
    {"name": "test/spectral_falls", "what": "The earlier audio waterfall, superseded by the stack: x is log frequency, so melodies visibly slide their peaks; every 0.42 s the live top ridge freezes and scrolls down, twenty-four ridges fading with age.", "status": "preview", "frames": ["test_spectral_falls_spacesong_t035.jpg", "test_spectral_falls_spacesong_t075.jpg"]},
    {"name": "test/wind_up", "what": "Dot to growing, precessing, breathing ellipses that speed up and grow harmonics, then collapse and settle into a standard calibration pulse. Spin is a precomputed cumulative integral of omega proportional to energy, so it visibly accelerates then decelerates.", "status": "preview", "frames": ["test_wind_up_t035.jpg", "test_wind_up_t075.jpg"]},
    {"name": "test/orbit_trace", "what": "One bright head orbits at steady angular rate; the only thing changing is the radial attraction versus angle, blended between shape profiles, so the TRAIL draws circle to triangle to square to star.", "status": "preview", "frames": ["test_orbit_trace_t035.jpg", "test_orbit_trace_t075.jpg"]},
    {"name": "test/lissajous_tour", "what": "Continuous morph through the classic Lissajous ratios with phase slowly sweeping, so each figure breathes.", "status": "preview", "frames": ["test_lissajous_tour_t035.jpg", "test_lissajous_tour_t075.jpg"]},
    {"name": "test/pulse_to_shape", "what": "The calibration pulse morphing into circle, square, triangle and star geometries and back.", "status": "preview", "frames": ["test_pulse_to_shape_t035.jpg", "test_pulse_to_shape_t075.jpg"]},
    {"name": "test/fourier_epicycles", "what": "DFT of a target outline into a chain of 48 rotating circles, an arm, and the traced curve. Swap the target for any closed path.", "status": "preview", "frames": ["test_fourier_epicycles_t035.jpg", "test_fourier_epicycles_t075.jpg"]}
  ],
  "parameters_intro": "The look is set almost entirely by four numbers on the scope and one on the filter.",
  "parameters": [
    {"name": "decay", "where": "RenderConfig / per composition", "meaning": "Afterglow length — the fraction of the phosphor buffer surviving each frame. Higher means longer ghost trails.", "range": "0.55 (audio scenes, where you want legibility) to 0.92 (attractor traces, where the figure IS the afterglow). 0.86 default."},
    {"name": "beam_width / glow_sigma / glow_gain", "where": "RenderConfig", "meaning": "Core thickness against bloom radius and strength.", "range": ""},
    {"name": "beam_color / glow_color", "where": "RenderConfig", "meaning": "Phosphor tint. P31 green by default; amber, magenta and pink have all been shipped.", "range": "Whatever you set here should also be handed to `film_config_for_phosphor` or the filter will blow the wrong channels out."},
    {"name": "trail_gain", "where": "per composition", "meaning": "How much light each frame's stroke adds.", "range": "60 fps traces read FAINTER than a 15 fps preview — a shorter arc is drawn per frame, so less light accumulates per lap. 0.40 is conservative, 0.55–0.70 is the punch-up lever."},
    {"name": "face_w_frac / face_h_frac", "where": "RenderConfig", "meaning": "Screen face size as a fraction of the frame. The ratio between them IS the deflection anisotropy.", "range": "This is the setting to change for a landscape screen."},
    {"name": "warmup", "where": "per composition", "meaning": "Seconds of the composition's TAIL pre-rolled into the phosphor before frame 0.", "range": "Required for any seamless loop whose first frame is not black, because the persistence buffer starts empty. About 1.6 s at 60 fps gives a seam difference around 0.2%."},
    {"name": "exposure", "where": "FilmLook", "meaning": "Filter exposure.", "range": "1.75 for a thin trace on black; ~0.72 for a full-frame bright image."},
    {"name": "level", "where": "GlitchFX", "meaning": "Fault rate and strength together.", "range": "0.8 subtle, 1.8 heavy"},
    {"name": "substeps", "where": "trail compositions", "meaning": "Sub-frame beam steps, so a fast dot leaves a CONTINUOUS trail rather than a dotted line.", "range": ""}
  ]
}
